/**
 * This component is a placeholder and doesn't render anything.
 * The theme toggle functionality is now handled by the LoginThemeToggle component
 * which creates a fixed position floating button.
 */
const ThemeToggle = () => {
  return null
}

export default ThemeToggle